# Reconly Demo Mode - Environment Configuration
#
# This file contains default settings for demo mode.
# These settings are optimized for a quick, self-contained demo experience.
#
# Copy to .env in the docker/demo/ directory to customize:
#   cp .env.demo .env
#
# Or use as-is - all values have sensible defaults.

# =============================================================================
# Demo Mode Settings
# =============================================================================

# Enable demo mode (shows demo banner, enables demo features)
RECONLY_DEMO_MODE=true

# Force reload seed data on startup (useful for resetting demo)
# Set to "true" to reload seed data even if database has data
DEMO_RESET=false

# =============================================================================
# Server Configuration
# =============================================================================

# Port to expose the API (default: 8000)
API_PORT=8000

# =============================================================================
# LLM Configuration (Ollama - bundled, no API keys needed)
# =============================================================================

# Default LLM provider (ollama is bundled in demo mode)
DEFAULT_PROVIDER=ollama

# Ollama server URL (points to bundled Ollama service)
OLLAMA_HOST=http://ollama:11434

# Ollama model to use (qwen2.5:3b is small, fast, and capable)
# This model is auto-pulled on first startup
OLLAMA_MODEL=qwen2.5:3b

# =============================================================================
# API Keys (NOT REQUIRED for demo mode)
# =============================================================================
# Leave these empty - demo mode uses bundled Ollama
# You can add keys here if you want to test with other providers

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
HUGGINGFACE_API_KEY=

# =============================================================================
# Edition
# =============================================================================

# Demo mode runs OSS edition
RECONLY_EDITION=oss

# =============================================================================
# Optional: Password Protection
# =============================================================================
# Uncomment to require password for demo access
# RECONLY_AUTH_PASSWORD=demo

# =============================================================================
# Advanced Settings (usually don't need to change)
# =============================================================================

# Database connection (uses bundled PostgreSQL)
# DATABASE_URL is set automatically in docker-compose.yml

# Timezone for scheduled tasks
# SCHEDULER_TIMEZONE=UTC
